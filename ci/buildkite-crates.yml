###
# Build steps that run on changes to cargo.toml and crate check scripts.
steps:
  - name: "publish crate test"
    command: "ci/publish-crate.sh --dry-run"
    agents:
      queue: "release-build"
    retry:
      manual:
        permit_on_passed: true
    timeout_in_minutes: 180
